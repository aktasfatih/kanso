node_modules/
js/
css/
vendor/
build/
*.tar.gz
# Signing material — never commit
*.key
*.crt
*.csr
appinfo/signature.json
.DS_Store
.phpunit.result.cache
.phpunit.cache/
test-results/
playwright-report/
# Captured auth session for e2e (regenerated by global-setup on every run)
tests/e2e/.auth/
.php-cs-fixer.cache
# Generated by dev/setup.sh from KANSO_DB (per-run DB driver env)
dev/.db.env
# Staging + state for dev/upgrade-check.sh (released tarball, unpacked trees,
# and the ids dev/seed.sh remembers between its seed and verify passes)
dev/.upgrade-app/
dev/.seed-state.env
.claude/
docs/design/
.playwright-mcp/
roadmap-preview.png
